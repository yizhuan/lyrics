import { Component, OnInit } from '@angular/core';
import { ActivatedRoute } from '@angular/router';
import { JhiDataUtils } from 'ng-jhipster';

import { ILyrics } from 'app/shared/model/lyrics.model';

@Component({
  selector: 'jhi-lyrics-detail',
  templateUrl: './lyrics-detail.component.html',
})
export class LyricsDetailComponent implements OnInit {
  lyrics: ILyrics | null = null;

  constructor(protected dataUtils: JhiDataUtils, protected activatedRoute: ActivatedRoute) {}

  ngOnInit(): void {
    this.activatedRoute.data.subscribe(({ lyrics }) => (this.lyrics = lyrics));
  }

  byteSize(base64String: string): string {
    return this.dataUtils.byteSize(base64String);
  }

  openFile(contentType = '', base64String: string): void {
    this.dataUtils.openFile(contentType, base64String);
  }

  previousState(): void {
    window.history.back();
  }
}
